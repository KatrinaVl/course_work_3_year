#!/bin/bash


    # line="<pv_distri>0<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_0

    # line="<pv_distri>1<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_1

    # line="<pv_distri>2<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_2

    # line="<pv_distri>3<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_3

    # line="<pv_distri>4<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_4

    # line="<pv_distri>5<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_5


    # line="<db_distri>0<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_0

    # line="<db_distri>1<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_1

    # line="<db_distri>2<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_2

    # line="<db_distri>3<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_3

    # line="<db_distri>4<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_4

    # line="<db_distri>5<\/db_distri>\t\t\t <!-- Disk speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<db_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/2_5

    # line="<max_speed>0.15<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_0

    # line="<max_speed>5<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_1

    # line="<max_speed>50<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_2

    # line="<max_speed>117.71<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_3

    # line="<max_speed>500<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_4

    # line="<max_speed>200<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_4

    # line="<max_speed>300<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_5

    # line="<max_speed>500<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/3_7

    # line="<max_speed>117.71<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>0.07<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/5_4

    # line="<pv_distri>1<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/1_1

    # line="<min_speed>300<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_1

    # line="<min_speed>100<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_2

    # line="<pb_param>0.1<\/pb_param>\t\t\t <!-- B -->" 
	# sed -i "s/<pb_param>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/4_0

    # line="<pb_param>0.5<\/pb_param>\t\t\t <!-- B -->" 
	# sed -i "s/<pb_param>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/4_1

    # line="<pb_param>0.9<\/pb_param>\t\t\t <!-- B -->" 
	# sed -i "s/<pb_param>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/4_2

    # line="<pv_distri>1<\/pv_distri>\t\t\t <!-- Speed fit distribution [ran_weibull, ran_gamma, ran_lognormal, normal, hyperx, exponential] -->" 
	# sed -i "s/<pv_distri>.*/${line}/" parameters.xml

    # line="<max_speed>0.5<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>0.1<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_1



    # line="<max_speed>4<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>1<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_2


    # line="<max_speed>10<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>5<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_3


    # line="<max_speed>30<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>12<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_4


    # line="<max_speed>50<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>30<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_5


    # line="<max_speed>200<\/max_speed>\t\t\t <!-- Maximum host speed in GFlops -->" 
	# sed -i "s/<max_speed>.*/${line}/" parameters.xml

    # line="<min_speed>60<\/min_speed>\t\t\t <!-- Minumum host speed in GFlops -->" 
	# sed -i "s/<min_speed>.*/${line}/" parameters.xml
	# ./generator
	# ./execute > performance/resourses/6_6